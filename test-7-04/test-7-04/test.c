#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

//
////操作符
//int main()
//{
////算术操作符
//	int a = 7 / 2; //整数除法，结果为3
//	//注意：如果需要浮点数除法，可以将其中一个操作数转换为浮点数
//	float a_1 = 7 / 2.0; //浮点数除法，结果为3.5
//	int b = 7 % 2; //取余，结果为1
//	//注意：取余操作符只能用于整数类型
//	int c = 7 + 2; //加法，结果为9
//	int d = 7 - 2; //减法，结果为5
//	int e = 7 * 2; //乘法，结果为14
//	int f = 7; // 初始化变量f为7
//	f++; // 后缀自增，结果为7，之后f变为8
//	int g = 7; // 初始化变量g为7
//	g--; // 后缀自减，结果为7，之后g变为6
//	int h = ++a; //前缀自增，结果为4，a变为4
//	int i = --b; //前缀自减，结果为0，b变为0
//
//	//位操作符
//	int j = 5 & 3; //按位与，结果为1
//	int k = 5 | 3; //按位或，结果为7
//	int l = 5 ^ 3; //按位异或，结果为6
//	int m = 5 << 1; //左移，结果为10
//	int n = 5 >> 1; //右移，结果为2
//	//注意：位操作符只能用于整数类型
//	int o = ~5; //按位取反，结果为-6（在二进制中，5的补码是11111010）
//
//	//赋值操作符
//	int p = 5; //简单赋值，p的值为5
//	p += 2; //加法赋值，p的值变为7
//	p -= 2; //减法赋值，p的值变为5
//
//	//单目操作符
//	//只有一个操作数的操作符
//	int q = 5; //简单赋值，q的值为5
//	q = +q; //正号操作符，结果为5
//	q = -q; //负号操作符，结果为-5
//	q = !q; //逻辑非操作符，结果为0（如果q为非零，则结果为0；如果q为零，则结果为1）
//	//注意：逻辑非操作符通常用于布尔值，但在C中也可以用于整数
//	int r = ~q; //按位取反操作符，结果为4（如果q为5，则结果为-6）
//	//注意：按位取反操作符只能用于整数类型
//	//sizeof操作符
//	int s = sizeof(int); //返回int类型的大小，通常为4字节
//	int t = sizeof(q); //返回变量q的类型大小，通常为4字节
//	/*注意：sizeof操作符在编译时计算，不会执行运行时计算
//	sizeof操作符可以用于任何数据类型，包括数组、结构体等
//	sizeof操作符也可以用于函数返回类型，但不能用于函数参数列表
//	注意：sizeof操作符不会计算函数的实际大小，只会返回函数指针的大小
//	sizeof操作符可以用于指针类型，但返回的是指针的大小，而不是指针所指向的数据类型的大小
//	注意：sizeof操作符不会计算指针所指向的数据类型的大小，只会返回指针本身的大小
//	sizeof操作符可以用于数组类型，但返回的是整个数组的大小，而不是数组元素的大小
//	注意：sizeof操作符会计算整个数组的大小，而不是单个元素的大小	
//	sizeof操作符可以用于结构体类型，但返回的是整个结构体的大小，而不是结构体成员的大小
//	注意：sizeof操作符会计算整个结构体的大小，而不是单个成员的大小
//	*/
//
//
////关系操作符
//	int u = (5 == 5); //等于操作符，结果为1（true）
//	int v = (5 != 3); //不等于操作符，结果为1（true）
//	int w = (5 > 3); //大于操作符，结果为1（true）
//	int x = (5 < 3); //小于操作符，结果为0（false）
//	int y = (5 >= 5); //大于等于操作符，结果为1（true）
//	int z = (5 <= 3); //小于等于操作符，结果为0（false）
////逻辑操作符
//	int aa = (5 > 3 && 3 < 7); //逻辑与操作符，结果为1（true）
//	int ab = (5 > 3 || 3 > 7); //逻辑或操作符，结果为1（true）
//	int ac = !(5 > 3); //逻辑非操作符，结果为0（false）
//
//
////其他操作符
//	int ad = (5 ? 10 : 20); //三元操作符，结果为10（如果条件为真，则返回第一个值，否则返回第二个值）



//	//常见关键字
//	//auto：自动类型推断（C11及以上版本）
//	//break：跳出循环或switch语句
//	//case：在switch语句中定义一个分支
//    //char：字符类型
//	//const：定义常量
//	//continue：跳过当前循环的剩余部分，进入下一次循环
//	//default：在switch语句中定义默认分支
//	//do：定义do-while循环
//	//double：双精度浮点数类型
//	//else：在if语句中定义else分支
//	//enum：定义枚举类型
//	//extern：声明外部变量或函数
//	//float：单精度浮点数类型	
//	//for：定义for循环
//	//goto：跳转到指定标签
//	//if：定义条件语句
//	//int：整数类型
//	//long：长整数类型
//	//register：请求编译器将变量存储在寄存器中（不保证）
//	//return：从函数返回值
//	//short：短整数类型
//	//signed：有符号整数类型
//	//sizeof：获取数据类型或变量的大小
//	//static：定义静态变量或函数
//	//struct：定义结构体类型
//	//switch：定义switch语句
//	//typedef：定义类型别名
//	
//
//
//	//union：定义联合类型
//	//unsigned：无符号整数类型
//	//void：无类型，表示函数不返回值或指针不指向任何类型
//	//volatile：告诉编译器变量可能会被异步修改，禁止优化
//	//while：定义while循环
//
//
//
//
//
//
//
//	return 0;
//}


//typedef unsigned int uint;
//
//typedef struct Node
//{
//	int data; //数据域
//	struct Node* next; //指向下一个节点的指针
//}Node;
//
//int main() {
//
//	unsigned int num = 10; //无符号整数类型
//	uint num2 = 20; //使用typedef定义的无符号整数类型别名
//	struct Node n;
//	Node n2;
//	return 0;
//}

//static
//1.修饰局部变量
//2.修饰全局变量
//3.修饰函数
/*1.局部变量改变为静态变量
  静态变量在函数调用结束后仍然保留其值，直到程序结束
  静态变量只会被初始化一次，之后的调用不会重新初始化
  静态变量的作用域仍然是局部的，但生命周期变为整个程序运行期间
  注意：静态变量在内存中分配在数据段而不是栈段
void test()//不返回值用void
{
	static int a = 1; //局部变量
	a++; //自增操作
	printf("a = %d\n", a); //输出局部变量a的值54
}

int main()
{
	int i = 0;
	while (i < 10)
	{
		test();
		i++;

	}


	return 0;
}*/


//2.修饰全局变量
// static修饰全局变量后，该变量只能在当前文件中访问，其他文件无法访问

/*extern int g_val; //声明全局变量g_val


int main()
{
	printf("%d\n", g_val); //输出全局变量g_val的值2025


	return 0;
}*/

//3.修饰函数
// static修饰函数后，该函数只能在当前文件中访问，其他文件无法访问
/*extern int Add(int x, int y); //声明Add函数

int main()
{
	int a = 10; //局部变量
	int b = 20; //局部变量
	int z = Add(a, b); //调用Add函数
	printf("%d\n", z); //输出Add函数的返回值30

	return 0;
}*/
//会报错：未定义的引用到Add函数，因为Add函数是静态函数，只能在当前文件中访问，其他文件无法访问

//register
//寄存器变量
/*int main()
{
	register int a = 10; //寄存器变量,建议放在寄存器中以提高访问速度

	
	return 0;
}
*/

//define定义常量和宏
//1.定义常量
/*#define num 10 //定义常量num为10
int main()
{
	printf("%d\n", num); //输出常量num的值10
	int arr[num] = { 0 }; //定义一个长度为num的数组
	return 0;
}*/


//2.定义宏
//宏是有参数的
#define ADD(x, y) ((x) + (y)) //定义宏ADD，计算x和y的和

int main()
{
	int a = 10; //局部变量
	int b = 20; //局部变量
	int c = ADD(a, b); //调用Add函数
	printf("%d\n", c); //输出Add函数的返回值30

	return 0;
}